#include "main"
/**
 * _islower - check if it is lowertcase or not
 * @c: character
 * Return: 1 if c ls lowertcase
 */
int _islower(int c)
{
	if (c >= 97 && c <= 122)
	{
	return (1);
	}
	else
	{
	return (0);
	}
}
