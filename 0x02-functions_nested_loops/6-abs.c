#include "main.h"
/**
*_abs - computes the absolute value
*@n: parameter or int to check
*Return: the absolute value of int
*/
int _abs(int n)
{
	if (n > 0)
	{
	return (n);
	}
	else if (n == 0)
	{
	return (0);
	}
	else
	{
	return (-n);
	}
}
