#include <stdio.h>
/* more headers goes there */

/* betty style doc for function main goes there */
int main(void)
{
	char n;

	for (n = 'z'; n >= 'a'; --n)
	putchar(n);

	putchar(10);
	return (0);
}
